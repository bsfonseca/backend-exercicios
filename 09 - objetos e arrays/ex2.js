// 2. Crie um objeto em JavaScript para colocar 5 atributos de um
// notebook. Atribua os seguintes atributos:
// Processador = i7
// Memória = 16GB
// Preço = 5000
// HD = 1TB
// SSD = 256GB
// Por fim, mostre o nome e valor de cada atributo no console,
// exatamente como está na atividade.

let notebook = {
  processador: "i7",
  memoria: "16GB",
  preco: 1000,
  HD: "1TB",
  SSD: "256GB",
};

console.log(`O processador é ${notebook.processador}`);
console.log(`A memoria é ${notebook.memoria}`);
console.log(`O preco é ${notebook.preco}`);
console.log(`O HD é ${notebook.HD}`);
console.log(`O SSD é ${notebook.SSD}`);
